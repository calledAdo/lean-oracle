/**
 * Placeholder thrown by scaffolded helpers until implementations land.
 *
 * Mirrors the ergonomics of Rust’s `todo!()` for quick discovery of gaps.
 *
 * @public
 */
export function todo(feature: string): never {
  throw new Error(`LeanOracleSdk: TODO — ${feature}`);
}
